# HTTP
LISTEN=:3000

# 필수
REDIS_URL=redis://localhost:6379

# 체인 (DEFAULT_CHAIN_ID, 체인별 RPC_URL_<chainId> 등)
DEFAULT_CHAIN_ID=56357
NONCE_LOCK_TTL_MS=30000

# 키 관리 (개인키 평문 금지 — KMS 경유)
KMS_ENDPOINT=

# 로깅: 운영(ELK)에서는 LOG_PRETTY=false → pino 포맷 JSON
LOG_LEVEL=debug
LOG_PRETTY=true
